<!-- Tab Pills -->
<div class="flex gap-2 mb-6 overflow-x-auto">
  <button
    onclick="switchTab('profile')"
    id="profileTab"
    class="tab-button flex items-center gap-2 px-4 py-2 rounded-full bg-blue-600 text-white transition-colors whitespace-nowrap"
  >
    <i class="ti ti-user"></i>
    <span>Profile</span>
  </button>
  <button
    onclick="switchTab('security')"
    id="securityTab"
    class="tab-button flex items-center gap-2 px-4 py-2 rounded-full bg-gray-700 text-gray-300 hover:bg-gray-600 transition-colors whitespace-nowrap"
  >
    <i class="ti ti-lock"></i>
    <span>Security</span>
  </button>
  <button
    onclick="switchTab('notification')"
    id="notificationTab"
    class="tab-button flex items-center gap-2 px-4 py-2 rounded-full bg-gray-700 text-gray-300 hover:bg-gray-600 transition-colors whitespace-nowrap"
  >
    <i class="ti ti-bell"></i>
    <span>Notification</span>
  </button>
  <button
    onclick="switchTab('oauth')"
    id="oauthTab"
    class="tab-button flex items-center gap-2 px-4 py-2 rounded-full bg-gray-700 text-gray-300 hover:bg-gray-600 transition-colors whitespace-nowrap"
  >
    <i class="ti ti-key"></i>
    <span>OAuth</span>
  </button>
</div>

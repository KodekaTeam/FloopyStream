<!-- Edit Stream Key Modal -->
<div
  id="editStreamKeyModal"
  class="fixed inset-0 bg-gray-600 bg-opacity-50 overflow-y-auto h-full w-full hidden z-50"
>
  <div
    class="relative top-20 mx-auto p-5 border w-full max-w-md shadow-lg rounded-lg bg-gray-800 border-gray-700"
  >
    <div class="flex items-center justify-between mb-4">
      <h3 class="text-lg font-semibold text-white">Edit Stream Key</h3>
      <button
        onclick="closeEditStreamKeyModal()"
        class="text-gray-400 hover:text-white transition-colors"
      >
        <i class="ti ti-x text-2xl"></i>
      </button>
    </div>

    <form id="editStreamKeyForm" class="space-y-4">
      <input type="hidden" id="editStreamKeyId" name="stream_key_id" />

      <div>
        <label class="block text-sm font-medium text-gray-300 mb-2"
          >Stream Key Name</label
        >
        <input
          type="text"
          id="editStreamKeyName"
          name="name"
          required
          class="w-full px-3 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent"
        />
      </div>

      <div>
        <label class="block text-sm font-medium text-gray-300 mb-2"
          >Description (Optional)</label
        >
        <textarea
          id="editStreamKeyDescription"
          name="description"
          rows="3"
          class="w-full px-3 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent"
        ></textarea>
      </div>

      <div>
        <label class="block text-sm font-medium text-gray-300 mb-2"
          >Streaming Protocol</label
        >
        <select
          id="editStreamingProtocol"
          name="streamingProtocol"
          class="w-full px-3 py-2 bg-gray-700 border border-gray-600 rounded-lg text-white focus:outline-none focus:ring-2 focus:ring-red-500 focus:border-transparent"
        >
          <option value="rtmp">RTMP (Recommended)</option>
          <option value="dash">DASH</option>
          <option value="hls">HLS</option>
        </select>
      </div>

      <div class="space-y-3">
        <div class="flex items-center">
          <input
            type="checkbox"
            id="editEnableManualSettings"
            name="enableManualSettings"
            class="w-4 h-4 text-red-600 bg-gray-700 border-gray-600 rounded focus:ring-red-500 focus:ring-2"
            disabled
          />
          <label
            for="editEnableManualSettings"
            class="ml-2 text-sm font-medium text-gray-300"
          >
            Turn on manual settings
            <span class="text-xs text-gray-500 ml-1"
              >(Cannot be changed after creation)</span
            >
          </label>
        </div>

        <!-- Manual Settings Options (shown if previously enabled) -->
        <div
          id="editManualSettingsOptions"
          class="space-y-3 ml-6 border-l-2 border-gray-600 pl-4"
          style="display: none"
        >
          <div>
            <label class="block text-sm font-medium text-gray-300 mb-2">
              Resolution
            </label>
            <select
              id="editManualResolution"
              name="manualResolution"
              class="w-full px-3 py-2 bg-gray-600 border border-gray-500 rounded-lg text-gray-400 cursor-not-allowed"
              disabled
            >
              <option value="1080p">1080p (Full HD)</option>
              <option value="720p">720p (HD)</option>
              <option value="480p">480p (SD)</option>
              <option value="360p">360p (Low)</option>
            </select>
            <p class="text-xs text-gray-500 mt-1">
              Resolution cannot be changed after stream key creation
            </p>
          </div>
        </div>

        <div class="flex items-center">
          <input
            type="checkbox"
            id="editEnable60fps"
            name="enable60fps"
            class="w-4 h-4 text-red-600 bg-gray-700 border-gray-600 rounded focus:ring-red-500 focus:ring-2"
            disabled
          />
          <label
            for="editEnable60fps"
            class="ml-2 text-sm font-medium text-gray-300"
          >
            Enable 60 FPS
            <span class="text-xs text-gray-500 ml-1"
              >(Cannot be changed after creation)</span
            >
          </label>
        </div>
      </div>

      <div class="flex justify-end space-x-3 pt-4">
        <button
          type="button"
          onclick="closeEditStreamKeyModal()"
          class="px-4 py-2 bg-gray-700 text-gray-300 rounded-lg hover:bg-gray-600 transition-colors"
        >
          Cancel
        </button>
        <button
          type="submit"
          class="px-4 py-2 bg-red-600 text-white rounded-lg hover:bg-red-700 transition-colors"
        >
          <i class="ti ti-device-floppy mr-2"></i>
          Update Stream Key
        </button>
      </div>
    </form>
  </div>
</div>
